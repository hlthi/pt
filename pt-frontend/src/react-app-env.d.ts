/// <reference types="react-scripts" />

interface IFish {
  id : number;
  name : string;
  image_url : string;
}

interface IStock {
  id : number;
  name : string;
  stock_amount : number,
  image_url : string;
  price : number;
}
